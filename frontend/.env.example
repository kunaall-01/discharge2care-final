# Copy this file to frontend/.env for local development.
# On Render, set REACT_APP_API_URL in the static site's Environment tab.
#
# Create React App inlines REACT_APP_* values at BUILD time, so changing this
# requires a rebuild and redeploy — it is not read at runtime.
REACT_APP_API_URL=http://localhost:8000
